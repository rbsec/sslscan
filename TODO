Properly parse the IMAP response and don't start until we see something like:

    ". OK Begin SSL/TLS negotiation now."
    ". OK Begin TLS negotiation now"

Properly parse expected XMPP responses by hand in a better way

Add tests for a given set of hosts that are known to be interesting
Add support for checking supported ciphers of a given Tor relay
Make a Debian package
Send diff from 1.8.2 to the upstream developer
